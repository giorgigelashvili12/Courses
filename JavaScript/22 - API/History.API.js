// back()
// loads back the last url that was opened
function myFunction() {
    window.history.back();
}

// go(x)
// go back to x pages
function myFunction() {
    window.history.go(-2);
}

// forward(x)
// go to next url, or x url
function myFunction() {
    window.history.forward(-2);
}