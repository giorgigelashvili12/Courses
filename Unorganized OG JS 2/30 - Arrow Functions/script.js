// Arrow functions allow us to define and write shorter function syntaxs
const addition = (a, b) => {return a + b};
// or
const subtraction = (a, b) => {
    return a - b;
}

// default
const message = () => 'Hello, World!';