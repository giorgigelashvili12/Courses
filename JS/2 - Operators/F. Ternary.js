// Ternary operators are a way to perform conditional expressions in 3 operations on a single line.
condition ? expressionIfTrue : expressionIfFalse;
// condition: if true then perform expressionIfTrue, else then expressionIfFalse

const testScore = 86;
score >= 80 ? 'A' : 'A-';
// if score is more than 80 return 'A' else return 'A-'

// In conclusion it is the shortest conditional expression